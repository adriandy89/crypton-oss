{
  "name": "@crypton/shared",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "Tipos, enums y contratos de API compartidos por api, worker y app.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "comments": {
    "consumo": "El backend (api/worker) consume dist/ compilado por tsc. La app Angular consume src/ directamente vía tsconfig paths, para no necesitar un build previo en cada cambio."
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "echo \"noop\" && exit 0"
  },
  "dependencies": {
    "decimal.js": "^10.6.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "rootDir": "src",
    "testRegex": "\\.spec\\.ts$"
  }
}
